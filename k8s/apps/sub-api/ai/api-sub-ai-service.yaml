apiVersion: v1
kind: Service
metadata:
  name: spring-sub-ai-api-service
  labels:
    app: spring-sub-ai-api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: spring-sub-ai-api
