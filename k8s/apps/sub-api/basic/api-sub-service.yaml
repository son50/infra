apiVersion: v1
kind: Service
metadata:
  name: spring-sub-api-service
  labels:
    app: spring-sub-api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: spring-sub-api
